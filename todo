
chat message suggestion for user based on board state. use the design-taste skill for this (not the v1 one)
let deposit number input field be empty / not have default value
update user balance as soon as message response done

instead of hint think time being slider let it be number input w/ plus minus buttons to the left and right of it. tapping increases the buttons, holding on buttons increases them continuously w/ 144ms interval. use the same design-taste skill for this


in view (do not implement)
    training modes
    easy way to show word meanings
    nararouter for chess
